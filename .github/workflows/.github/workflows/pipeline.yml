name: Full Pipeline

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Test OK"

  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - run: echo "Deploy OK"